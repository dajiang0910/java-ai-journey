services:
  redis-stack:
    image: redis/redis-stack:latest
    container_name: notes-redis-stack
    ports:
      - "6379:6379"   # Redis 服务端口
      - "8001:8001"   # RedisInsight 图形化管理界面
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
